name: Hello World

on:
    push:
        branches:
            [main]
jobs:
    hello:
        runs-on: ubuntu-22.04
        steps:
            - name: Hello World
              run: echo "Hello, World!"

            - name: Ver fechas del runner
              run: date

            - name: Ver archivos disponibles
              run: ls -la
